import React from "react";
import classes from "./Conference.module.css";

const Conference = (props) => {
	return <section className={classes.sectionContainer}>Conference</section>;
};

export default Conference;
